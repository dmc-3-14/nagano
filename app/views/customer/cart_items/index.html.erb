<h5>ショッピングカート</h5>
  	
  	<%= link_to "カートを空にする", alldestroy_cart_item_path, method: :delete, "data-confirm" => "カートを空にしますか？", class: "btn btn-danger" %>
  	
  	<table class='table'>
  	   
  	   <thead>
		    <tr>
		     <th></th>
			 <th>商品名</th>
			 <th>単価(税込)</th>
			 <th>数量</th>
			 <th>小計</th>
			 <th></th>
		     </tr>
	   </thead>
	   
  	<%# @cart_items.each do |cart| %>
  	   <tbody>
  		  <tr>
  		     <td><%#= attachment_image_tag cart.item, :image, :fill, 60, 60, format:'jpeg', fallback: "no_image.jpg" %>
  		     <td><%#= cart.item.title %></td>
  		     <td><%#= crat.item.non_tax_price.to_i * 1.1 %></td>
  		     <td>
  		         <%#= form_with @cart_item do |f| %>
                 <%#= f.select :amount,(1..20).to_a %>
                 <%#= f.submit "変更", class: "btn btn-primary" %>
                 <%# end %>
  		     </td>
  		     <td><%#= (cart.item.non_tax_price * cart.cart_item.amount * 1.1) %></td>　
             <td><%= link_to 'Destroy', cart_item_path(cart_item.id), method: :delete,  class: "btn btn-sm btn-danger"%></td>
         </tr>
       <tbody>
    <% end %>
    </table>
    
    <div class="col-2">
    	<%= link_to '買い物を続ける', items_path, class: "btn btn-info btn-block btn-md" %>
    </div>
           
    <br>
    <table class='table table-bordered col-2'> 
          <tr>
             <td class="table-active col-2">合計金額<td>
	      </tr>
	</table>
	       
    <div class="col-2">
    	<%= link_to '情報入力に進む', put_order_path(current_customer), class: "btn btn-primary btn-block btn-md" %>
    <div>