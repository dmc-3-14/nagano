カスタマー
<h1><strong>商品一覧</strong>(全<%= @items.count %>件)</h1>

<% @items.each do |item| %>
<div>
  <%= attachment_image_tag item, :image %>
  <p><%= item.title %></p>
  <p>¥<%= item.non_tax_price %></p>
</div>
<% end %>